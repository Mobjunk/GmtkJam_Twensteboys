using System.Collections;
using System.Collections.Generic;
using UnityEngine;

//put this on a go and it will select the parrent instead
public class InvalidSelection : MonoBehaviour
{
    //we have nothing to do here just so we can force parrent select when we somehow selected this
}
